#include<iostream>
using namespace std;

void reverse(int n) {
    if(n == 0) {
        return ;
    } else {
        reverse(n - 1);
        cout << n <<" ";
    }
}
int main() {
    reverse(10);
    return 0;
}
